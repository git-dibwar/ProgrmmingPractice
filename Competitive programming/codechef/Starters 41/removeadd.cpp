#include<bits/stdc++.h>
using namespace std;

int main(){
int t;
cin>>t;
while(t--){
    
    int n;
    int a[n];
    while(int i=0;i<n;i++){
        cin>>a[i];
    }
    set<int>(set.begin(),set.end()).size()


    int ans=0;
    sort(a,a+n);
    if(a.size()-(a.start,a.end).size())
}
return 0;
}